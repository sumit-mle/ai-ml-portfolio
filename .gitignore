# Python
.venv/
**/.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Secrets — never commit
.env
**/.env
*.pem
*.key
*credentials*

# Per-project caches / generated artifacts (each project also has its own .gitignore)
**/data/
**/output/
**/logs/
**/.cache/

# NOTE: results/<file>.json (eval summaries) are intentionally COMMITTED so
# recruiters can see real verified numbers. Per-project .gitignore files
# already exclude per-run artifacts under results/runs/ when needed.

# Project 04 (CrewAI) per-project venv (Python 3.13)
04-multi-agent-research-crew/.venv/

# Project 06 demo artifacts
06-mcp-tool-server-collection/auth/tokens.json
06-mcp-tool-server-collection/data/

# Project 08 generated PDFs (regenerated by reportlab on first run)
08-browser-agent-autonomous/data/portal/

# Tracing / observability
*.log
.trace/

# Internal handoff doc — not for public repo
HANDOFF.md

# Profile README draft — staged for the sumit-mle/sumit-mle special repo
_profile_readme_draft.md
_profile_repo/
