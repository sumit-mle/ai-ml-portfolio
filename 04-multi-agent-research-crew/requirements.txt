# Core
python-dotenv>=1.0.0
pydantic>=2.9.0
requests>=2.32.0

# CrewAI 0.x is the stable line as of mid-2026. Python 3.10-3.13 only.
crewai>=0.86.0
crewai-tools>=0.25.0

# Tavily web search (drop-in for CrewAI's built-in SerperDevTool — Tavily is
# more sales-research friendly and has a free tier).
tavily-python>=0.5.0

# LLM glue
openai>=1.50.0
langchain-openai>=0.2.0
