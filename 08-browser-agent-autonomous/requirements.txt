# Core
python-dotenv>=1.0.0
pydantic>=2.9.0
tenacity>=9.0.0

# Self-hosted compliance portal (demo target)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
reportlab>=4.0.0          # generate compliance PDFs at runtime

# Deterministic browser automation
playwright>=1.50.0

# Autonomous agent (LLM-driven browser)
browser-use>=0.12.0

# LLM glue (used by both the agent and the eval)
openai>=2.0.0
